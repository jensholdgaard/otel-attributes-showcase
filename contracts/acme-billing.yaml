# OTelSDK Contract - Auto-generated by Crossplane
# This is the source of truth for the acme-billing SDK
# DO NOT EDIT MANUALLY - changes will be overwritten
#
# This file defines:
#   1. What semantic conventions to include (attributeSelector)
#   2. What languages to generate (languages + package config)
#   3. Backstage catalog metadata (catalog section)

apiVersion: otel.example.com/v1alpha1
kind: OTelSDKContract
metadata:
  name: acme-billing

spec:
  # SDK identification
  displayName: "ACME Billing SDK"
  description: "Semantic conventions for ACME billing operations"
  version: "1.0.0"
  owner: "billing-team"
  domain: "billing"

  # Attribute selectors - patterns to match semconv files
  # Examples: "dk.mycompany.*", "custom.billing.*"
  attributeSelector:
    - "dk.mycompany.*"

  # Target languages and their package configuration
  languages:
    - go
    - dotnet

  packages:
    go:
      module: "github.com/backstage-opa-otel/acme-billing-semconv"
      outputPath: "sdks/go/acme-billing"
    dotnet:
      namespace: "Acme.Billing.SemanticConventions"
      outputPath: "sdks/dotnet/acme-billing"

# Backstage catalog metadata
catalog:
  apiVersion: backstage.io/v1alpha1
  kind: Component
  metadata:
    name: acme-billing
    title: "ACME Billing SDK"
    description: "Semantic conventions for ACME billing operations"
    annotations:
      github.com/project-slug: "backstage-opa-otel/otel-attributes-showcase"
      otel.example.com/sdk-version: "1.0.0"
      otel.example.com/contract-path: "contracts/acme-billing.yaml"
    tags:
      - otel
      - sdk
      - observability
      - go
      - dotnet
    links:
      - url: https://pkg.go.dev/github.com/backstage-opa-otel/acme-billing-semconv
        title: Go Documentation
        icon: docs
      - url: https://www.nuget.org/packages/Acme.Billing.SemanticConventions
        title: NuGet Package
        icon: package
  spec:
    type: library
    lifecycle: production
    owner: "billing-team"
    domain: "billing"
    providesApis: []
    dependsOn: []
